<?xml version="1.0"?>
<prefab>
    <!-- all the built-in components this actor has -->
    <components>
        <component type="sprite">
            <properties
                name="sprite"
                atlas="rocks"
                scale="1.2" 
                />
        </component>
        <component type="rigidbody">
            <properties name="rigidbody" />
        </component>
        <component type="circlecollider">
            <properties radius="11" />
        </component>
    </components>
    
    <!-- all the lua scripts that make up this actor -->
    <scripts>
        <script file="small_asteroid.lua" />
        <script file="asteroid.lua" />
        <script file="wrap.lua" />
    </scripts>
    
    <!-- query tags -->
    <tags>
        <tag>asteroid</tag>
    </tags>
</prefab>