<?xml version="1.0"?>
<prefab>
    <!-- all the built-in components this actor has -->
    <components>
        <component type="sprite">
            <properties
                name="sprite"
                atlas="rocks"
                scale="1.2" 
                />
        </component>
        <component type="rigidbody">
            <properties name="rigidbody" />
        </component>
        <component type="circlecollider">
            <properties radius="11" />
        </component>
    </components>
    
    <!-- all the lua scripts that make up this actor -->
    <behaviors>
        <behavior file="small_asteroid.lua" />
        <behavior file="asteroid.lua" />
        <behavior file="wrap.lua" />
    </behaviors>
    
    <!-- query tags -->
    <tags>
        <tag>asteroid</tag>
    </tags>
</prefab>